extends layout

block content
  .container(style='margin-left: 30%;margin-right: 30%;width:40%;text-align: center;')
    if (!account)
      h1 Sign in to your account
      p.lead You’re one step away from discovering the universe
      a.btn.btn-primary.btn-lg.btn-block(href='/login', role='button') Login
      br
      a.btn.btn-default.btn-lg.btn-block(href='/register', role='button') Register
    if (account)
      h1 Have fun!